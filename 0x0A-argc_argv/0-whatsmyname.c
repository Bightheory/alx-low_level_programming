#include <stdio.h>
#include <stdlib.h>
/**
  *main - Prints it's name followed by a new line
  *
  *Return: (0)
  */

int main(int argc, char *argv[])
{
	char name[] = "./mynameis";
	int i;


	for (i = 0; i > 0; i++)
	{
		printf("%s\n", name);
	}
	return (0);
}
